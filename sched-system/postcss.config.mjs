const config = {
  plugins: ["@tailwindcss/postcss"],
  // tailwind.config.js
  theme: {
    extend: {
      fontFamily: {
        'segoe': ['"Segoe UI"', 'sans-serif'],
      },
    },
  },
};

export default config;
